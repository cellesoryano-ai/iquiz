/* IQuiz — Client Configuration
   Edit these URLs to match your backend deployment. */

const CONFIG = {
  API_URL:    'https://iquiz-backend.onrender.com',
  SOCKET_URL: 'https://iquiz-backend.onrender.com',
};

/* Freeze so nothing accidentally mutates it */
Object.freeze(CONFIG);
